# CV 2024

## Commands

```bash
$ docker compose up --build
$ docker compose run --rm latex latexmk -pdf -pvc cv.tex
$ docker compose run --rm latex latexmk -c
```

Copyright 2025, Seyyed Ali Mohammadiyeh (MAX BASE)
